- 参与了testkey的测试过程

    - 能够独立实现探针台上wafer和对准的相关操作；
    - T800机台程序的debug调试、运行和条件设置；
    - 不同die，不同坐标下cell的读取、写入和擦除操作；
         
- data的输出
    data的输出文件为TXT格式，首先需要导入Excel中，再进行筛选与绘图。
    然而，导入到Excel中的data对绘图很不友好，而数据量很大，需要耗费大量时间进行重复性筛选操作。
    为了节省大家处理data的时间，我特意使用python对Excel中的数据进行了自动化筛选，以此提高data处理的效率。
    通过借助xlwings和panda等科学计算工具包，python可以实现对Excel数据进行自动化处理甚至是绘图。
- data的分析结果
    器件的pgm过程容易达成，阈值电压值发生明显shift，而且电流区间在可以接受的范围内；
    器件的ers过程往往为一次性的过程，导致了再次pgm过程的失败，而且其选中的BL所有cells是破坏性的，可能是HCI效应诱发的氧化层界面和drain端的界面发生了破坏；
    调换BL和SL顺序，分别对BL和SL进行pgm和ers操作，均可重复完成。这说明，ers的破坏力仅限于drain端，对ers本身影响较小。
    此时，有两种解决办法，一种是想办法降低ers操作电压，降低其破坏性；第二种是想办法使得ers的破坏点原理drain端。前者局限于阈值电压shift的窗口值会减小，后者局限于ers效率的降低，
    也会伴随着窗口值的降低。还有一种尝试，是通过使用脉冲电压的形式，降低ers的破坏力，这就需要重新更改coding和测试条件。
    
